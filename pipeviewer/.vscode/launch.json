{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/target/debug/pipeviewer",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    
    ]
}